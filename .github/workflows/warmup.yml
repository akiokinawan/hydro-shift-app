name: Backend Warmup

on:
  schedule:
    # 15分ごとに実行
    - cron: '*/15 * * * *'

jobs:
  warmup:
    runs-on: ubuntu-latest
    steps:
      - name: Send warmup request to Render backend
        run: |
          curl -s https://hydro-shift-app.onrender.com/api/warmup
